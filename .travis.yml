notifications:
  email: 
    false
dist: trusty
sudo: required
language:
  - cpp
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-6
      - g++-6
script:
  - gcc -std=c++1y -c Source/Core/*.cpp
  - g++ -std=c++1y -c Source/Core/*.cpp